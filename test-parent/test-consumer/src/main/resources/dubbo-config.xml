<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="test"  />
 
	<dubbo:registry protocol="zookeeper" address="127.0.0.1:2181"/>
    <!-- 生成远程服务代理，可以和本地bean一样使用demoService -->   
    <dubbo:protocol name="dubbo" port="${aries.protocol.aries.port}" />
    <!--消费方的缺省值 -->
    <dubbo:consumer loadbalance="leastactive" check="false" init="false"  retries="0"/>
    
    <!--<dubbo:provider version="80089"
		protocol="dubbo" loadbalance="leastactive" cluster="failfast" timeout="60000"
		layer="service" />-->	
 	<import resource="classpath:rmi/msf-dubbo-consumer.xml" /> 
</beans>